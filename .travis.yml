sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -f frontend/Dockerfile.dev . -t lmachraf/frontend

script:
  - docker run lmachraf/frontend npm start test -- --coverage